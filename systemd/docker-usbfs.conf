[Unit]
# usbcore is built-in, so /etc/modprobe.d/usbfs-memory.conf is ignored.
# Force the unlimited USBFS cap before any SDR container claims a dongle.
After=usbfs-memory.service
Wants=usbfs-memory.service
